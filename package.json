{
  "name": "site-template",
  "version": "2025.04.05",
  "private": true,
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "typegen": "bun run --filter '*' clean && bun run --filter './studio' typegen && cp ./studio/schema.json ./next-app && bun run --filter './next-app' typegen",
    "test:update": "bun run --filter '*' clean && cp ./studio/package.json ./studio/package.test.json && cp ./next-app/package.json ./next-app/package.test.json && bun update --recursive --latest --no-cache --dry-run --verbose --no-save",
    "full:update": "bun run --filter '*' clean && bun update --recursive --latest --no-cache && bun install --recursive",
    "clean": "rm -rf **/node_modules .turbo .next .vercel .cache dist *.log"
  },
  "workspaces": [
    "next-app",
    "studio",
    "shared-lib"
  ]
}
