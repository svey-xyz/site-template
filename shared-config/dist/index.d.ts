/**
 * As this file is reused in several other files, try to keep it lean and small.
 * Importing other npm packages here could lead to needlessly increasing the client bundle size, or end up in a server-only function that don't need it.
 */
export declare const dataset: string;
export declare const projectId: string;
export declare const apiVersion: string;
export declare const configStudioBasePath: string;
export declare const configStudioName: string;
export declare const configStudioTitle: string;
export declare const sanityStudioPreviewURL: string;
export declare const sanityStudioHost: string;
export declare const config: {
    projectId: string;
    dataset: string;
    baseUrl: string;
};
